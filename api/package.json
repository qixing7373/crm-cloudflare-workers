{
  "name": "crm-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply crm-api --local",
    "db:migrate:remote": "wrangler d1 migrations apply crm-api --remote",
    "db:seed:local": "wrangler d1 execute crm-api --local --file src/seed.sql",
    "db:seed:remote": "wrangler d1 execute crm-api --remote --file src/seed.sql",
    "smoke:prod": "bun ../scripts/smoke-production.mjs",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "db:generate": "drizzle-kit generate",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "drizzle-orm": "^0.39.0",
    "hono": "^4.7.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/bun": "^1.3.11",
    "drizzle-kit": "^0.30.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.80.0"
  }
}
